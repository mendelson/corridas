pytest==8.3.0
pytest-playwright==0.5.0
pytest-json-report==1.5.0
# Parallel test execution (one worker per runner core) — the Playwright browser
# suite dominates wall time; the live_server fixture binds an OS-assigned port
# per worker so parallel runs don't collide.
pytest-xdist==3.6.1
# Needed so tests can import scraper.merger / scraper.utils (pure-logic unit tests
# that don't hit the network). Keep versions in sync with requirements.txt.
unidecode==1.3.8
python-dateutil==2.9.0
