pytest==8.3.0
pytest-playwright==0.5.0
pytest-json-report==1.5.0
# Needed so tests can import scraper.merger / scraper.utils (pure-logic unit tests
# that don't hit the network). Keep versions in sync with requirements.txt.
unidecode==1.3.8
python-dateutil==2.9.0
